/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReportCreateCompanyPostingsReportRequestFilter model module.
 * @module model/ReportCreateCompanyPostingsReportRequestFilter
 * @version 2.1
 */
class ReportCreateCompanyPostingsReportRequestFilter {
    /**
     * Constructs a new <code>ReportCreateCompanyPostingsReportRequestFilter</code>.
     * Фильтр.
     * @alias module:model/ReportCreateCompanyPostingsReportRequestFilter
     */
    constructor() { 
        
        ReportCreateCompanyPostingsReportRequestFilter.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReportCreateCompanyPostingsReportRequestFilter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportCreateCompanyPostingsReportRequestFilter} obj Optional instance to populate.
     * @return {module:model/ReportCreateCompanyPostingsReportRequestFilter} The populated <code>ReportCreateCompanyPostingsReportRequestFilter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportCreateCompanyPostingsReportRequestFilter();

            if (data.hasOwnProperty('cancel_reason_id')) {
                obj['cancel_reason_id'] = ApiClient.convertToType(data['cancel_reason_id'], ['Number']);
            }
            if (data.hasOwnProperty('delivery_schema')) {
                obj['delivery_schema'] = ApiClient.convertToType(data['delivery_schema'], ['String']);
            }
            if (data.hasOwnProperty('offer_id')) {
                obj['offer_id'] = ApiClient.convertToType(data['offer_id'], 'String');
            }
            if (data.hasOwnProperty('processed_at_from')) {
                obj['processed_at_from'] = ApiClient.convertToType(data['processed_at_from'], 'Date');
            }
            if (data.hasOwnProperty('processed_at_to')) {
                obj['processed_at_to'] = ApiClient.convertToType(data['processed_at_to'], 'Date');
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], ['Number']);
            }
            if (data.hasOwnProperty('status_alias')) {
                obj['status_alias'] = ApiClient.convertToType(data['status_alias'], ['String']);
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], ['Number']);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportCreateCompanyPostingsReportRequestFilter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportCreateCompanyPostingsReportRequestFilter</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['cancel_reason_id'])) {
            throw new Error("Expected the field `cancel_reason_id` to be an array in the JSON data but got " + data['cancel_reason_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['delivery_schema'])) {
            throw new Error("Expected the field `delivery_schema` to be an array in the JSON data but got " + data['delivery_schema']);
        }
        // ensure the json data is a string
        if (data['offer_id'] && !(typeof data['offer_id'] === 'string' || data['offer_id'] instanceof String)) {
            throw new Error("Expected the field `offer_id` to be a primitive type in the JSON string but got " + data['offer_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sku'])) {
            throw new Error("Expected the field `sku` to be an array in the JSON data but got " + data['sku']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['status_alias'])) {
            throw new Error("Expected the field `status_alias` to be an array in the JSON data but got " + data['status_alias']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['statuses'])) {
            throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}



/**
 * Идентификатор причины отмены.
 * @member {Array.<Number>} cancel_reason_id
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['cancel_reason_id'] = undefined;

/**
 * Схема работы — FBO или FBS.  Чтобы получить отчёт по схеме FBO, передайте `fbo` в этом параметре. Для отчёта по схеме FBS передайте `fbs`. 
 * @member {Array.<String>} delivery_schema
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['delivery_schema'] = undefined;

/**
 * Артикул товара.
 * @member {String} offer_id
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['offer_id'] = undefined;

/**
 * Время, когда заказ попал в обработку.
 * @member {Date} processed_at_from
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['processed_at_from'] = undefined;

/**
 * Время, когда заказ появился в личном кабинете.
 * @member {Date} processed_at_to
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['processed_at_to'] = undefined;

/**
 * Идентификатор товара в системе Ozon.
 * @member {Array.<Number>} sku
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['sku'] = undefined;

/**
 * Текст статуса.
 * @member {Array.<String>} status_alias
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['status_alias'] = undefined;

/**
 * Числовой статус.
 * @member {Array.<Number>} statuses
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['statuses'] = undefined;

/**
 * Название товара.
 * @member {String} title
 */
ReportCreateCompanyPostingsReportRequestFilter.prototype['title'] = undefined;






export default ReportCreateCompanyPostingsReportRequestFilter;

