/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RatingValue from './RatingValue';

/**
 * The V1Rating model module.
 * @module model/V1Rating
 * @version 2.1
 */
class V1Rating {
    /**
     * Constructs a new <code>V1Rating</code>.
     * @alias module:model/V1Rating
     */
    constructor() { 
        
        V1Rating.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>V1Rating</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/V1Rating} obj Optional instance to populate.
     * @return {module:model/V1Rating} The populated <code>V1Rating</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V1Rating();

            if (data.hasOwnProperty('danger_threshold')) {
                obj['danger_threshold'] = ApiClient.convertToType(data['danger_threshold'], 'Number');
            }
            if (data.hasOwnProperty('premium_threshold')) {
                obj['premium_threshold'] = ApiClient.convertToType(data['premium_threshold'], 'Number');
            }
            if (data.hasOwnProperty('rating')) {
                obj['rating'] = ApiClient.convertToType(data['rating'], 'String');
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], [RatingValue]);
            }
            if (data.hasOwnProperty('warning_threshold')) {
                obj['warning_threshold'] = ApiClient.convertToType(data['warning_threshold'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>V1Rating</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>V1Rating</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['rating'] && !(typeof data['rating'] === 'string' || data['rating'] instanceof String)) {
            throw new Error("Expected the field `rating` to be a primitive type in the JSON string but got " + data['rating']);
        }
        if (data['values']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['values'])) {
                throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
            }
            // validate the optional field `values` (array)
            for (const item of data['values']) {
                RatingValue.validateJsonObject(item);
            };
        }

        return true;
    }


}



/**
 * Пороговое значение рейтинга, после которого продажи будут заблокированы.
 * @member {Number} danger_threshold
 */
V1Rating.prototype['danger_threshold'] = undefined;

/**
 * Пороговое значение рейтинга для участия в Premium-программе.
 * @member {Number} premium_threshold
 */
V1Rating.prototype['premium_threshold'] = undefined;

/**
 * Системное название рейтинга.
 * @member {String} rating
 */
V1Rating.prototype['rating'] = undefined;

/**
 * Список значений рейтинга.
 * @member {Array.<module:model/RatingValue>} values
 */
V1Rating.prototype['values'] = undefined;

/**
 * Пороговое значение рейтинга, после которого появится предупреждение о возможной блокировке.
 * @member {Number} warning_threshold
 */
V1Rating.prototype['warning_threshold'] = undefined;






export default V1Rating;

