/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChatExternalUser from './ChatExternalUser';

/**
 * The ChatChat model module.
 * @module model/ChatChat
 * @version 2.1
 */
class ChatChat {
    /**
     * Constructs a new <code>ChatChat</code>.
     * @alias module:model/ChatChat
     */
    constructor() { 
        
        ChatChat.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ChatChat</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChatChat} obj Optional instance to populate.
     * @return {module:model/ChatChat} The populated <code>ChatChat</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChatChat();

            if (data.hasOwnProperty('first_unread_message_id')) {
                obj['first_unread_message_id'] = ApiClient.convertToType(data['first_unread_message_id'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('last_message_id')) {
                obj['last_message_id'] = ApiClient.convertToType(data['last_message_id'], 'Number');
            }
            if (data.hasOwnProperty('unread_count')) {
                obj['unread_count'] = ApiClient.convertToType(data['unread_count'], 'Number');
            }
            if (data.hasOwnProperty('users')) {
                obj['users'] = ApiClient.convertToType(data['users'], [ChatExternalUser]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChatChat</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChatChat</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['users']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['users'])) {
                throw new Error("Expected the field `users` to be an array in the JSON data but got " + data['users']);
            }
            // validate the optional field `users` (array)
            for (const item of data['users']) {
                ChatExternalUser.validateJsonObject(item);
            };
        }

        return true;
    }


}



/**
 * Идентификатор первого непрочитанного сообщения в чате.
 * @member {Number} first_unread_message_id
 */
ChatChat.prototype['first_unread_message_id'] = undefined;

/**
 * Идентификатор чата.
 * @member {String} id
 */
ChatChat.prototype['id'] = undefined;

/**
 * Идентификатор последнего сообщения в чате.
 * @member {Number} last_message_id
 */
ChatChat.prototype['last_message_id'] = undefined;

/**
 * Количество непрочитанных сообщений в чате.
 * @member {Number} unread_count
 */
ChatChat.prototype['unread_count'] = undefined;

/**
 * Cписок участников чата.
 * @member {Array.<module:model/ChatExternalUser>} users
 */
ChatChat.prototype['users'] = undefined;






export default ChatChat;

