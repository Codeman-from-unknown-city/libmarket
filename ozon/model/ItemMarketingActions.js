/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MarketingActionsMarketingAction from './MarketingActionsMarketingAction';

/**
 * The ItemMarketingActions model module.
 * @module model/ItemMarketingActions
 * @version 2.1
 */
class ItemMarketingActions {
    /**
     * Constructs a new <code>ItemMarketingActions</code>.
     * @alias module:model/ItemMarketingActions
     */
    constructor() { 
        
        ItemMarketingActions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ItemMarketingActions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ItemMarketingActions} obj Optional instance to populate.
     * @return {module:model/ItemMarketingActions} The populated <code>ItemMarketingActions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ItemMarketingActions();

            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], [MarketingActionsMarketingAction]);
            }
            if (data.hasOwnProperty('current_period_from')) {
                obj['current_period_from'] = ApiClient.convertToType(data['current_period_from'], 'Date');
            }
            if (data.hasOwnProperty('current_period_to')) {
                obj['current_period_to'] = ApiClient.convertToType(data['current_period_to'], 'Date');
            }
            if (data.hasOwnProperty('ozon_actions_exist')) {
                obj['ozon_actions_exist'] = ApiClient.convertToType(data['ozon_actions_exist'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ItemMarketingActions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ItemMarketingActions</code>.
     */
    static validateJSON(data) {
        if (data['actions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['actions'])) {
                throw new Error("Expected the field `actions` to be an array in the JSON data but got " + data['actions']);
            }
            // validate the optional field `actions` (array)
            for (const item of data['actions']) {
                MarketingActionsMarketingAction.validateJsonObject(item);
            };
        }

        return true;
    }


}



/**
 * Маркетинговые акции продавца. Параметры `date_from`, `date_to`, `discount_value` и `title` указываются для каждой акции продавца.
 * @member {Array.<module:model/MarketingActionsMarketingAction>} actions
 */
ItemMarketingActions.prototype['actions'] = undefined;

/**
 * Дата и время начала текущего периода по всем действующим акциям.
 * @member {Date} current_period_from
 */
ItemMarketingActions.prototype['current_period_from'] = undefined;

/**
 * Дата и время окончания текущего периода по всем действующим акциям.
 * @member {Date} current_period_to
 */
ItemMarketingActions.prototype['current_period_to'] = undefined;

/**
 * Если к товару может быть применена акция за счёт Ozon — `true`.
 * @member {Boolean} ozon_actions_exist
 */
ItemMarketingActions.prototype['ozon_actions_exist'] = undefined;






export default ItemMarketingActions;

