/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Fbsv4PostingProductDetailWithoutDimensions model module.
 * @module model/Fbsv4PostingProductDetailWithoutDimensions
 * @version 2.1
 */
class Fbsv4PostingProductDetailWithoutDimensions {
    /**
     * Constructs a new <code>Fbsv4PostingProductDetailWithoutDimensions</code>.
     * @alias module:model/Fbsv4PostingProductDetailWithoutDimensions
     */
    constructor() { 
        
        Fbsv4PostingProductDetailWithoutDimensions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Fbsv4PostingProductDetailWithoutDimensions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Fbsv4PostingProductDetailWithoutDimensions} obj Optional instance to populate.
     * @return {module:model/Fbsv4PostingProductDetailWithoutDimensions} The populated <code>Fbsv4PostingProductDetailWithoutDimensions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Fbsv4PostingProductDetailWithoutDimensions();

            if (data.hasOwnProperty('mandatory_mark')) {
                obj['mandatory_mark'] = ApiClient.convertToType(data['mandatory_mark'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('offer_id')) {
                obj['offer_id'] = ApiClient.convertToType(data['offer_id'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'Number');
            }
            if (data.hasOwnProperty('currency_code')) {
                obj['currency_code'] = ApiClient.convertToType(data['currency_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Fbsv4PostingProductDetailWithoutDimensions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Fbsv4PostingProductDetailWithoutDimensions</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['mandatory_mark'])) {
            throw new Error("Expected the field `mandatory_mark` to be an array in the JSON data but got " + data['mandatory_mark']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['offer_id'] && !(typeof data['offer_id'] === 'string' || data['offer_id'] instanceof String)) {
            throw new Error("Expected the field `offer_id` to be a primitive type in the JSON string but got " + data['offer_id']);
        }
        // ensure the json data is a string
        if (data['price'] && !(typeof data['price'] === 'string' || data['price'] instanceof String)) {
            throw new Error("Expected the field `price` to be a primitive type in the JSON string but got " + data['price']);
        }
        // ensure the json data is a string
        if (data['currency_code'] && !(typeof data['currency_code'] === 'string' || data['currency_code'] instanceof String)) {
            throw new Error("Expected the field `currency_code` to be a primitive type in the JSON string but got " + data['currency_code']);
        }

        return true;
    }


}



/**
 * Обязательная маркировка «Честный ЗНАК».
 * @member {Array.<String>} mandatory_mark
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['mandatory_mark'] = undefined;

/**
 * Название товара.
 * @member {String} name
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['name'] = undefined;

/**
 * Идентификатор товара в системе продавца — артикул.
 * @member {String} offer_id
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['offer_id'] = undefined;

/**
 * Цена.
 * @member {String} price
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['price'] = undefined;

/**
 * Количество товара в отправлении.
 * @member {Number} quantity
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['quantity'] = undefined;

/**
 * Идентификатор товара в системе Ozon — SKU.
 * @member {Number} sku
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['sku'] = undefined;

/**
 * Валюта ваших цен. Cовпадает с валютой, которая установлена в настройках личного кабинета.  Возможные значения:    - `RUB` — российский рубль,   - `BYN` — белорусский рубль,   - `KZT` — тенге,   - `EUR` — евро,   - `USD` — доллар США,   - `CNY` — юань. 
 * @member {String} currency_code
 */
Fbsv4PostingProductDetailWithoutDimensions.prototype['currency_code'] = undefined;






export default Fbsv4PostingProductDetailWithoutDimensions;

