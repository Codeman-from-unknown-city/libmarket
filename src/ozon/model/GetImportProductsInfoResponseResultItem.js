/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import V1ItemError from './V1ItemError';

/**
 * The GetImportProductsInfoResponseResultItem model module.
 * @module model/GetImportProductsInfoResponseResultItem
 * @version 2.1
 */
class GetImportProductsInfoResponseResultItem {
    /**
     * Constructs a new <code>GetImportProductsInfoResponseResultItem</code>.
     * @alias module:model/GetImportProductsInfoResponseResultItem
     */
    constructor() { 
        
        GetImportProductsInfoResponseResultItem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GetImportProductsInfoResponseResultItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetImportProductsInfoResponseResultItem} obj Optional instance to populate.
     * @return {module:model/GetImportProductsInfoResponseResultItem} The populated <code>GetImportProductsInfoResponseResultItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetImportProductsInfoResponseResultItem();

            if (data.hasOwnProperty('offer_id')) {
                obj['offer_id'] = ApiClient.convertToType(data['offer_id'], 'String');
            }
            if (data.hasOwnProperty('product_id')) {
                obj['product_id'] = ApiClient.convertToType(data['product_id'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [V1ItemError]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetImportProductsInfoResponseResultItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetImportProductsInfoResponseResultItem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['offer_id'] && !(typeof data['offer_id'] === 'string' || data['offer_id'] instanceof String)) {
            throw new Error("Expected the field `offer_id` to be a primitive type in the JSON string but got " + data['offer_id']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                V1ItemError.validateJsonObject(item);
            };
        }

        return true;
    }


}



/**
 * Идентификатор товара в системе продавца — артикул.
 * @member {String} offer_id
 */
GetImportProductsInfoResponseResultItem.prototype['offer_id'] = undefined;

/**
 * Идентификатор товара.
 * @member {Number} product_id
 */
GetImportProductsInfoResponseResultItem.prototype['product_id'] = undefined;

/**
 * Статус создания товара. Информация о товаре обрабатывается очередями. Возможные значения параметра: - `pending` — товар в очереди на обработку; - `imported` — товар успешно загружен; - `failed` — товар загружен с ошибками. 
 * @member {String} status
 */
GetImportProductsInfoResponseResultItem.prototype['status'] = undefined;

/**
 * Массив ошибок.
 * @member {Array.<module:model/V1ItemError>} errors
 */
GetImportProductsInfoResponseResultItem.prototype['errors'] = undefined;






export default GetImportProductsInfoResponseResultItem;

