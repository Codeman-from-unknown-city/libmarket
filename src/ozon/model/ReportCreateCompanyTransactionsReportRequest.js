/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReportCreateCompanyTransactionsReportRequest model module.
 * @module model/ReportCreateCompanyTransactionsReportRequest
 * @version 2.1
 */
class ReportCreateCompanyTransactionsReportRequest {
    /**
     * Constructs a new <code>ReportCreateCompanyTransactionsReportRequest</code>.
     * @alias module:model/ReportCreateCompanyTransactionsReportRequest
     */
    constructor() { 
        
        ReportCreateCompanyTransactionsReportRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReportCreateCompanyTransactionsReportRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportCreateCompanyTransactionsReportRequest} obj Optional instance to populate.
     * @return {module:model/ReportCreateCompanyTransactionsReportRequest} The populated <code>ReportCreateCompanyTransactionsReportRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportCreateCompanyTransactionsReportRequest();

            if (data.hasOwnProperty('date_from')) {
                obj['date_from'] = ApiClient.convertToType(data['date_from'], 'String');
            }
            if (data.hasOwnProperty('date_to')) {
                obj['date_to'] = ApiClient.convertToType(data['date_to'], 'String');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
            if (data.hasOwnProperty('search')) {
                obj['search'] = ApiClient.convertToType(data['search'], 'String');
            }
            if (data.hasOwnProperty('transaction_type')) {
                obj['transaction_type'] = ApiClient.convertToType(data['transaction_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportCreateCompanyTransactionsReportRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportCreateCompanyTransactionsReportRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['date_from'] && !(typeof data['date_from'] === 'string' || data['date_from'] instanceof String)) {
            throw new Error("Expected the field `date_from` to be a primitive type in the JSON string but got " + data['date_from']);
        }
        // ensure the json data is a string
        if (data['date_to'] && !(typeof data['date_to'] === 'string' || data['date_to'] instanceof String)) {
            throw new Error("Expected the field `date_to` to be a primitive type in the JSON string but got " + data['date_to']);
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }
        // ensure the json data is a string
        if (data['search'] && !(typeof data['search'] === 'string' || data['search'] instanceof String)) {
            throw new Error("Expected the field `search` to be a primitive type in the JSON string but got " + data['search']);
        }
        // ensure the json data is a string
        if (data['transaction_type'] && !(typeof data['transaction_type'] === 'string' || data['transaction_type'] instanceof String)) {
            throw new Error("Expected the field `transaction_type` to be a primitive type in the JSON string but got " + data['transaction_type']);
        }

        return true;
    }


}



/**
 * Дата, с которой рассчитывается отчёт по транзакциям. Формат UTC: ГГГГ-ММ-ДД. 
 * @member {String} date_from
 */
ReportCreateCompanyTransactionsReportRequest.prototype['date_from'] = undefined;

/**
 * Дата, по которую рассчитывается отчёт по транзакциям. Формат UTC: ГГГГ-ММ-ДД. 
 * @member {String} date_to
 */
ReportCreateCompanyTransactionsReportRequest.prototype['date_to'] = undefined;

/**
 * Язык ответа:   - `RU` — русский,   - `EN` — английский. 
 * @member {String} language
 * @default 'DEFAULT'
 */
ReportCreateCompanyTransactionsReportRequest.prototype['language'] = 'DEFAULT';

/**
 * Поиск по содержанию записи, проверяет наличие.
 * @member {String} search
 */
ReportCreateCompanyTransactionsReportRequest.prototype['search'] = undefined;

/**
 * Фильтр по типу транзакции:   - `ALL` — все,   - `ORDERS` — заказы,   - `RETURNS` — возвраты,   - `SERVICES` — сервисные сборы,   - `DEPOSIT` — депозит,   - `OTHER` — другие. 
 * @member {String} transaction_type
 * @default 'ALL'
 */
ReportCreateCompanyTransactionsReportRequest.prototype['transaction_type'] = 'ALL';






export default ReportCreateCompanyTransactionsReportRequest;

