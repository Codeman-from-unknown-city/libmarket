/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GetProductInfoStocksV3ResponseStock from './GetProductInfoStocksV3ResponseStock';

/**
 * The Productv3GetProductInfoStocksV3ResponseItem model module.
 * @module model/Productv3GetProductInfoStocksV3ResponseItem
 * @version 2.1
 */
class Productv3GetProductInfoStocksV3ResponseItem {
    /**
     * Constructs a new <code>Productv3GetProductInfoStocksV3ResponseItem</code>.
     * @alias module:model/Productv3GetProductInfoStocksV3ResponseItem
     */
    constructor() { 
        
        Productv3GetProductInfoStocksV3ResponseItem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Productv3GetProductInfoStocksV3ResponseItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Productv3GetProductInfoStocksV3ResponseItem} obj Optional instance to populate.
     * @return {module:model/Productv3GetProductInfoStocksV3ResponseItem} The populated <code>Productv3GetProductInfoStocksV3ResponseItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Productv3GetProductInfoStocksV3ResponseItem();

            if (data.hasOwnProperty('offer_id')) {
                obj['offer_id'] = ApiClient.convertToType(data['offer_id'], 'String');
            }
            if (data.hasOwnProperty('product_id')) {
                obj['product_id'] = ApiClient.convertToType(data['product_id'], 'Number');
            }
            if (data.hasOwnProperty('stocks')) {
                obj['stocks'] = ApiClient.convertToType(data['stocks'], [GetProductInfoStocksV3ResponseStock]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Productv3GetProductInfoStocksV3ResponseItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Productv3GetProductInfoStocksV3ResponseItem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['offer_id'] && !(typeof data['offer_id'] === 'string' || data['offer_id'] instanceof String)) {
            throw new Error("Expected the field `offer_id` to be a primitive type in the JSON string but got " + data['offer_id']);
        }
        if (data['stocks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stocks'])) {
                throw new Error("Expected the field `stocks` to be an array in the JSON data but got " + data['stocks']);
            }
            // validate the optional field `stocks` (array)
            for (const item of data['stocks']) {
                GetProductInfoStocksV3ResponseStock.validateJsonObject(item);
            };
        }

        return true;
    }


}



/**
 * Идентификатор товара в системе продавца — артикул.
 * @member {String} offer_id
 */
Productv3GetProductInfoStocksV3ResponseItem.prototype['offer_id'] = undefined;

/**
 * Идентификатор товара.
 * @member {Number} product_id
 */
Productv3GetProductInfoStocksV3ResponseItem.prototype['product_id'] = undefined;

/**
 * Информация об остатках.
 * @member {Array.<module:model/GetProductInfoStocksV3ResponseStock>} stocks
 */
Productv3GetProductInfoStocksV3ResponseItem.prototype['stocks'] = undefined;






export default Productv3GetProductInfoStocksV3ResponseItem;

