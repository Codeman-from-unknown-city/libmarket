/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OperationPosting model module.
 * @module model/OperationPosting
 * @version 2.1
 */
class OperationPosting {
    /**
     * Constructs a new <code>OperationPosting</code>.
     * Информация об отправлении.
     * @alias module:model/OperationPosting
     */
    constructor() { 
        
        OperationPosting.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OperationPosting</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OperationPosting} obj Optional instance to populate.
     * @return {module:model/OperationPosting} The populated <code>OperationPosting</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OperationPosting();

            if (data.hasOwnProperty('delivery_schema')) {
                obj['delivery_schema'] = ApiClient.convertToType(data['delivery_schema'], 'String');
            }
            if (data.hasOwnProperty('order_date')) {
                obj['order_date'] = ApiClient.convertToType(data['order_date'], 'String');
            }
            if (data.hasOwnProperty('posting_number')) {
                obj['posting_number'] = ApiClient.convertToType(data['posting_number'], 'String');
            }
            if (data.hasOwnProperty('warehouse_id')) {
                obj['warehouse_id'] = ApiClient.convertToType(data['warehouse_id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OperationPosting</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OperationPosting</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['delivery_schema'] && !(typeof data['delivery_schema'] === 'string' || data['delivery_schema'] instanceof String)) {
            throw new Error("Expected the field `delivery_schema` to be a primitive type in the JSON string but got " + data['delivery_schema']);
        }
        // ensure the json data is a string
        if (data['order_date'] && !(typeof data['order_date'] === 'string' || data['order_date'] instanceof String)) {
            throw new Error("Expected the field `order_date` to be a primitive type in the JSON string but got " + data['order_date']);
        }
        // ensure the json data is a string
        if (data['posting_number'] && !(typeof data['posting_number'] === 'string' || data['posting_number'] instanceof String)) {
            throw new Error("Expected the field `posting_number` to be a primitive type in the JSON string but got " + data['posting_number']);
        }

        return true;
    }


}



/**
 * Схема доставки:   - `FBO` — доставка со склада Ozon,   - `FBS` — доставка со своего склада,   - `RFBS` — доставка по выбору продавца,   - `Crossborder` — доставка из-за рубежа. 
 * @member {String} delivery_schema
 */
OperationPosting.prototype['delivery_schema'] = undefined;

/**
 * Дата принятия отправления в обработку.
 * @member {String} order_date
 */
OperationPosting.prototype['order_date'] = undefined;

/**
 * Номер отправления.
 * @member {String} posting_number
 */
OperationPosting.prototype['posting_number'] = undefined;

/**
 * Идентификатор склада.
 * @member {Number} warehouse_id
 */
OperationPosting.prototype['warehouse_id'] = undefined;






export default OperationPosting;

