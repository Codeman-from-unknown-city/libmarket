/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Productv2GetProductListResponseItem from './Productv2GetProductListResponseItem';

/**
 * The Productv2GetProductListResponseResult model module.
 * @module model/Productv2GetProductListResponseResult
 * @version 2.1
 */
class Productv2GetProductListResponseResult {
    /**
     * Constructs a new <code>Productv2GetProductListResponseResult</code>.
     * Результат.
     * @alias module:model/Productv2GetProductListResponseResult
     */
    constructor() { 
        
        Productv2GetProductListResponseResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Productv2GetProductListResponseResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Productv2GetProductListResponseResult} obj Optional instance to populate.
     * @return {module:model/Productv2GetProductListResponseResult} The populated <code>Productv2GetProductListResponseResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Productv2GetProductListResponseResult();

            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [Productv2GetProductListResponseItem]);
            }
            if (data.hasOwnProperty('last_id')) {
                obj['last_id'] = ApiClient.convertToType(data['last_id'], 'String');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Productv2GetProductListResponseResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Productv2GetProductListResponseResult</code>.
     */
    static validateJSON(data) {
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                Productv2GetProductListResponseItem.validateJsonObject(item);
            };
        }
        // ensure the json data is a string
        if (data['last_id'] && !(typeof data['last_id'] === 'string' || data['last_id'] instanceof String)) {
            throw new Error("Expected the field `last_id` to be a primitive type in the JSON string but got " + data['last_id']);
        }

        return true;
    }


}



/**
 * Список товаров.
 * @member {Array.<module:model/Productv2GetProductListResponseItem>} items
 */
Productv2GetProductListResponseResult.prototype['items'] = undefined;

/**
 * Идентификатор последнего значения на странице.  Чтобы получить следующие значения, передайте полученное значение в следующем запросе в параметре `last_id`. 
 * @member {String} last_id
 */
Productv2GetProductListResponseResult.prototype['last_id'] = undefined;

/**
 * Всего товаров.
 * @member {Number} total
 */
Productv2GetProductListResponseResult.prototype['total'] = undefined;






export default Productv2GetProductListResponseResult;

