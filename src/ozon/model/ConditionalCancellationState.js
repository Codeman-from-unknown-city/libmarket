/**
 * Документация Ozon Seller API
 * По вопросам работы с Seller API, обращайтесь в поддержку через личный кабинет.  > [Инструкции по работе с маркетплейсом](https://seller-edu.ozon.ru) 
 *
 * The version of the OpenAPI document: 2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ConditionalCancellationState model module.
 * @module model/ConditionalCancellationState
 * @version 2.1
 */
class ConditionalCancellationState {
    /**
     * Constructs a new <code>ConditionalCancellationState</code>.
     * Статус заявки на отмену.
     * @alias module:model/ConditionalCancellationState
     */
    constructor() { 
        
        ConditionalCancellationState.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ConditionalCancellationState</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ConditionalCancellationState} obj Optional instance to populate.
     * @return {module:model/ConditionalCancellationState} The populated <code>ConditionalCancellationState</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ConditionalCancellationState();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ConditionalCancellationState</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ConditionalCancellationState</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * Идентификатор статуса.
 * @member {Number} id
 */
ConditionalCancellationState.prototype['id'] = undefined;

/**
 * Название статуса.
 * @member {String} name
 */
ConditionalCancellationState.prototype['name'] = undefined;

/**
 * Статус заявки: - `ON_APPROVAL` — ожидает решения. - `APPROVED` — подтверждённая. - `REJECTED` — отклонённая. 
 * @member {module:model/ConditionalCancellationState.StateEnum} state
 */
ConditionalCancellationState.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
ConditionalCancellationState['StateEnum'] = {

    /**
     * value: "ON_APPROVAL"
     * @const
     */
    "ON_APPROVAL": "ON_APPROVAL",

    /**
     * value: "APPROVED"
     * @const
     */
    "APPROVED": "APPROVED",

    /**
     * value: "REJECTED"
     * @const
     */
    "REJECTED": "REJECTED"
};



export default ConditionalCancellationState;

